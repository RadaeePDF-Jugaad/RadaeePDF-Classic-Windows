<UserControl
    x:Class="RDPDFReader.annotui.PopSearch"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RDPDFReader.annotui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="150"
    d:DesignWidth="220">

    <RelativePanel x:Name="mPanel" Padding="10,10,10,10">
        <TextBox x:Name="mKey" TextWrapping="NoWrap" Width="200"/>
        <CheckBox x:Name="mCase" RelativePanel.Below="mKey" Content="Match Case" />
        <CheckBox x:Name="mWord" RelativePanel.Below="mCase" Content="Whole word" />
        <Button x:Name="mPrev" Content="Previous" Width="100" RelativePanel.Below="mWord"/>
        <Button x:Name="mNext" Content="Next" Width="100" RelativePanel.RightOf="mPrev" RelativePanel.Below="mWord"/>
    </RelativePanel>
</UserControl>
